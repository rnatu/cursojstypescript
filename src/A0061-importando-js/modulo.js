// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
export function soma(x, y) {
  return x + y;
}

//! quando for mudar um arquivo muito grande de js para ts,
//! desabilidar o strict no tsconfig.json para false só para inicialmente conseguir trabalhar sem muitos erros
//! mas o objetivo final é manter o strict como true
